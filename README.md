# Anonymous Chat Backend

Backend API для приложения Anonymous Chat, построенный на Express, TypeScript и MongoDB.

## 🚀 Технологии

- **Express** - веб-фреймворк
- **TypeScript** - типизированный JavaScript
- **MongoDB** - база данных
- **Swagger** - документация API
- **Winston** - логирование
- **Zod** - валидация данных
- **Helmet** - безопасность заголовков
- **CORS** - настройка CORS
- **Express Rate Limit** - защита от спама
- **Sentry** - мониторинг ошибок

## 📋 Требования

- Node.js >= 18
- MongoDB >= 6.0
- npm или yarn

## 🛠️ Установка

1. Установите зависимости:
```bash
npm install
# или
yarn install
```

2. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

3. Настройте переменные окружения в `.env`

4. Запустите MongoDB (локально или через Docker)

## 🏃 Запуск

### Разработка
```bash
npm run dev
# или
yarn dev
```

### Продакшн
```bash
npm run build
npm start
# или
yarn build
yarn start
```

### Docker
```bash
docker-compose up -d
```

## 📚 API Документация

После запуска сервера, документация Swagger доступна по адресу:
```
http://localhost:3001/api-docs
```

## 🔄 API Версионирование

API использует версионирование для обеспечения обратной совместимости:

- **V1 API**: `/api/v1/*` - текущая версия API
- **Обратная совместимость**: `/api/*` - перенаправляется на v1 (для старых клиентов)

Примеры:
```
GET /api/v1/health
GET /api/health  # Работает через обратную совместимость
```

## 🏥 Health Check

Проверка состояния сервера:
```
GET /api/v1/health
GET /api/health  # Обратная совместимость
```

## 🔒 Безопасность

- **Helmet** - защита заголовков HTTP
- **CORS** - настройка кросс-доменных запросов
- **Rate Limiting** - ограничение количества запросов
- **Валидация данных** - проверка входящих данных через Zod
- **Обработка ошибок** - централизованная обработка ошибок

## 📝 Логирование

Логи сохраняются в папке `logs/`:
- `error.log` - только ошибки
- `combined.log` - все логи

## 🧪 Тестирование

Проект включает unit, integration и E2E тесты:

```bash
# Запустить все тесты
npm test
# или
yarn test

# Запустить тесты в watch режиме
npm run test:watch

# Запустить тесты с покрытием
npm run test:coverage

# Запустить только unit тесты
npm run test:unit

# Запустить только integration тесты
npm run test:integration

# Запустить E2E тесты (полные сценарии)
npm run test:e2e
# или
yarn test:e2e
```

### Типы тестов

- **Unit тесты** - тестирование отдельных утилит (JWT, password, cache)
- **Integration тесты** - тестирование API endpoints
- **E2E тесты** - полные пользовательские сценарии

### Настройка тестовой БД

Тесты используют отдельную тестовую БД. Убедитесь, что в `.env` или переменных окружения указан:
```
TEST_MONGODB_URI=mongodb://localhost:27017/anonymous-chat-test
```

## 🧪 Линтинг и форматирование

```bash
# Проверка линтера
npm run lint

# Автоисправление
npm run lint:fix

# Форматирование
npm run format

# Проверка форматирования
npm run format:check
```

## 🐳 Docker

### Варианты использования

#### 1. С локальным MongoDB (для разработки)
```bash
# Запуск с локальным MongoDB
docker-compose --profile local-db up -d

# Или для разработки с hot-reload
docker-compose -f docker-compose.dev.yml up -d
```

#### 2. С MongoDB Atlas (продакшн)
```bash
# Убедитесь, что в .env указан MONGODB_URI для MongoDB Atlas
# Запуск без локального MongoDB
docker-compose up -d
```

#### 3. Продакшн с локальным MongoDB (персистентное хранилище)
```bash
# Создайте директорию для данных MongoDB
sudo mkdir -p /srv/mongo-data
sudo chown -R 999:999 /srv/mongo-data

# Запуск продакшн конфигурации
docker-compose -f docker-compose.prod.yml up -d
```

**📖 Подробная документация по настройке продакшна:** см. [PRODUCTION.md](./PRODUCTION.md)

#### 4. Сборка образа вручную
```bash
docker build -t anonymous-chat-backend .
```

### Переменные окружения в Docker

Docker Compose автоматически загружает переменные из `.env` файла. Убедитесь, что в `.env` указаны:
- `MONGODB_URI` - URI подключения к MongoDB (локальный или Atlas)
- `JWT_SECRET` - секретный ключ для JWT
- `JWT_EXPIRES_IN` - время жизни токена
- `FRONTEND_URL` - URL фронтенда (используется для CORS и генерации ссылок)
- И другие необходимые переменные

### Остановка контейнеров
```bash
# Для разработки
docker-compose -f docker-compose.dev.yml down

# Для продакшна
docker-compose -f docker-compose.prod.yml down

# С удалением volumes (удалит данные MongoDB)
docker-compose -f docker-compose.prod.yml down -v
```

### Бэкапы MongoDB

Для продакшна настроены скрипты бэкапов:

```bash
# Автоматический бэкап через mongodump (без остановки MongoDB)
./scripts/backup-mongodb.sh

# Восстановление из бэкапа
./scripts/restore-mongodb.sh /srv/mongo-backups/mongodb_backup_YYYYMMDD_HHMMSS.gz

# Бэкап через копирование данных (требует остановки MongoDB)
./scripts/backup-mongodb-data.sh
```

Подробнее см. [PRODUCTION.md](./PRODUCTION.md)

## 🔄 Миграции базы данных

Система миграций автоматически применяется при запуске сервера, что позволяет безопасно обновлять схему БД без потери данных.

### Автоматический запуск

Миграции запускаются автоматически при старте сервера (в режимах development и production).

### Ручное управление

#### Проверить статус миграций
```bash
yarn migrate:status
```

#### Запустить миграции вручную
```bash
yarn migrate
```

#### Откатить миграцию
```bash
yarn migrate:rollback <migration-name>
```

### Создание новой миграции

1. Создайте файл в `src/migrations/files/` с именем в формате `XXX_description.ts`
2. Экспортируйте функции `up` и `down`:

```typescript
export const up = async (): Promise<void> => {
  // Код для применения миграции
};

export const down = async (): Promise<void> => {
  // Код для отката миграции (опционально)
};
```

Подробнее см. `src/migrations/README.md`

## 📁 Структура проекта

```
src/
├── config/          # Конфигурация (env, database, swagger)
├── middleware/      # Middleware (errorHandler, validation, rateLimiter)
├── routes/          # Маршруты API
├── utils/           # Утилиты (logger, AppError)
├── models/          # Модели MongoDB (Mongoose)
├── controllers/     # Контроллеры
├── services/        # Бизнес-логика
├── app.ts           # Настройка Express приложения
└── server.ts        # Точка входа
```

## 🔧 Переменные окружения

### Основные
- `NODE_ENV` - окружение (development/production)
- `PORT` - порт сервера (по умолчанию: 3001)
- `MONGODB_URI` - URI подключения к MongoDB
- `FRONTEND_URL` - URL фронтенда (используется для CORS и генерации ссылок в email, по умолчанию: http://localhost:3000)
- `JWT_SECRET` - секретный ключ для JWT токенов
- `JWT_EXPIRES_IN` - время жизни JWT токена (по умолчанию: 7d)
- `RATE_LIMIT_WINDOW_MS` - окно для rate limiting
- `RATE_LIMIT_MAX_REQUESTS` - максимальное количество запросов
- `SENTRY_DSN` - DSN для Sentry (опционально)
- `SENTRY_ENVIRONMENT` - окружение для Sentry
- `LOG_LEVEL` - уровень логирования

### Redis (для кэширования)
- `REDIS_ENABLED` - включить Redis (true/false, по умолчанию: false)
- `REDIS_HOST` - хост Redis (по умолчанию: localhost)
- `REDIS_PORT` - порт Redis (по умолчанию: 6379)
- `REDIS_PASSWORD` - пароль Redis (опционально)

**Примечание:** Если Redis не включен или недоступен, система автоматически использует in-memory кэш (fallback).

### Email/SMTP настройки (для восстановления пароля)
- `SMTP_HOST` - SMTP сервер (например: smtp.gmail.com, smtp.mail.ru, smtp.yandex.ru)
- `SMTP_PORT` - SMTP порт (обычно 587 для TLS или 465 для SSL, по умолчанию: 587)
- `SMTP_USER` - Email адрес для отправки писем
- `SMTP_PASSWORD` - Пароль от email или пароль приложения
- `SMTP_FROM` - Отображаемое имя отправителя (опционально, по умолчанию используется SMTP_USER)
- `SMTP_SECURE` - Использовать SSL (true/false, по умолчанию: false)

**Примечание:** В development режиме, если SMTP не настроен, токен восстановления пароля будет возвращен в ответе для тестирования. В production режиме SMTP обязателен.

## 📡 API Endpoints

Все endpoints доступны через `/api/v1/` или `/api/` (обратная совместимость).

### Аутентификация
- `POST /api/v1/auth/login` - Вход в систему
- `POST /api/v1/auth/register` - Регистрация пользователя
- `POST /api/v1/auth/forgot-password` - Запрос на восстановление пароля (отправляет email)
- `POST /api/v1/auth/reset-password` - Сброс пароля по токену
- `POST /api/v1/auth/verify-password` - Проверка пароля компании
- `GET /api/v1/auth/me` - Получить текущего пользователя

### Сообщения
- `GET /api/v1/messages` - Получить все сообщения (с фильтром по companyCode)
- `GET /api/v1/messages/:id` - Получить сообщение по ID
- `POST /api/v1/messages` - Создать новое сообщение
- `PUT /api/v1/messages/:id/status` - Обновить статус сообщения

### Компании
- `GET /api/v1/companies` - Получить все компании (только для админов)
- `GET /api/v1/companies/:id` - Получить компанию по ID
- `GET /api/v1/companies/code/:code` - Получить компанию по коду (публичный)
- `GET /api/v1/companies/public` - Получить список публичных компаний (для sitemap)
- `POST /api/v1/companies` - Создать компанию (только для админов)
- `PUT /api/v1/companies/:id` - Обновить компанию
- `PUT /api/v1/companies/:id/status` - Обновить статус компании (только для админов)
- `PUT /api/v1/companies/:id/plan` - Обновить план компании (только для админов)

### Статистика
- `GET /api/v1/stats/company/:id` - Получить статистику компании
- `GET /api/v1/stats/distribution/:id` - Получить распределение сообщений по типам
- `GET /api/v1/stats/growth/:id` - Получить метрики роста компании
- `GET /api/v1/stats/achievements/:id` - Получить достижения компании
- `GET /api/v1/stats/achievements/:id/grouped` - Получить сгруппированные достижения
- `GET /api/v1/stats/platform` - Получить статистику платформы (только для админов)

### Планы подписки
- `GET /api/v1/plans` - Получить все планы (публичный)
- `POST /api/v1/plans` - Создать план (только для админов)
- `GET /api/v1/plans/free-settings` - Получить настройки бесплатного плана
- `PUT /api/v1/plans/free-settings` - Обновить настройки бесплатного плана (только для админов)

### Администраторы
- `GET /api/v1/admins` - Получить всех админов (только для суперадминов)
- `POST /api/v1/admins` - Создать админа (только для суперадминов)
- `PUT /api/v1/admins/:id` - Обновить админа (только для суперадминов)

## 🔐 Аутентификация

Большинство endpoints требуют аутентификации через JWT токен. Токен должен быть передан в заголовке:
```
Authorization: Bearer <token>
```

Роли пользователей:
- `user` - обычный пользователь
- `company` - администратор компании
- `admin` - администратор платформы
- `super_admin` - суперадминистратор

## 💾 Кэширование

Система использует двухуровневое кэширование:

1. **Redis** (production) - распределенный кэш для кластеров
2. **In-memory** (fallback) - локальный кэш, если Redis недоступен

Кэш автоматически переключается между Redis и in-memory в зависимости от доступности. TTL для разных типов данных:
- Компании: 10 минут
- Статистика: 2 минуты
- Сообщения: 1 минута
- По умолчанию: 5 минут

Для включения Redis установите `REDIS_ENABLED=true` в `.env`.

## 📧 Настройка Email для восстановления пароля

### Варианты настройки SMTP

#### 1. Gmail
```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password  # Используйте пароль приложения, не обычный пароль!
SMTP_FROM=FeedbackHub <your-email@gmail.com>
FRONTEND_URL=http://localhost:3000
```

**Важно для Gmail:** Нужно создать [пароль приложения](https://myaccount.google.com/apppasswords), обычный пароль не подойдет.

#### 2. Mail.ru
```env
SMTP_HOST=smtp.mail.ru
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your-email@mail.ru
SMTP_PASSWORD=your-password
SMTP_FROM=FeedbackHub <your-email@mail.ru>
FRONTEND_URL=http://localhost:3000
```

#### 3. Yandex
```env
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your-email@yandex.ru
SMTP_PASSWORD=your-password
SMTP_FROM=FeedbackHub <your-email@yandex.ru>
FRONTEND_URL=http://localhost:3000
```

#### 4. SendGrid (рекомендуется для продакшна)
```env
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=apikey
SMTP_PASSWORD=your-sendgrid-api-key
SMTP_FROM=FeedbackHub <noreply@yourdomain.com>
FRONTEND_URL=https://yourdomain.com
```

### Development режим

В development режиме, если SMTP не настроен:
- Токен восстановления пароля будет возвращен в ответе API для тестирования
- Email не будет отправлен
- Это позволяет тестировать функционал без настройки SMTP

### Production режим

В production режиме SMTP обязателен. Без него восстановление пароля не будет работать.

## 📄 Лицензия

ISC

# Anonymous-chat-backend



