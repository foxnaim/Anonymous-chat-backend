# Dockerfile для разработки с hot-reload
FROM node:20-alpine

WORKDIR /app

# Установка зависимостей
COPY package.json yarn.lock* package-lock.json* ./
RUN yarn install

# Создание директории для логов
RUN mkdir -p logs

EXPOSE 3001

# Используем tsx для hot-reload в разработке
CMD ["yarn", "dev"]

